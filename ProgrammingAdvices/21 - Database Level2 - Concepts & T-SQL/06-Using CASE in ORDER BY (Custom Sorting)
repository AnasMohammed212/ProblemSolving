select * from Sales 
Order by 
case
when SaleAmount > 150 then 1
else 2
end,SaleAmount




SELECT *, 
    CASE 
        WHEN SaleAmount > 150 THEN 1 
        ELSE 2 
    END AS SaleLevel 
FROM Sales 
ORDER BY SaleLevel,SaleAmount;
